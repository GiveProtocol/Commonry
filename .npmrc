# Prevent pre-release versions
tag-version-prefix=""
save-exact=true

# Use production registry only
registry=https://registry.npmjs.org/

# Allow peer dependency mismatches during ESLint 10 transition
# eslint-plugin-react and eslint-plugin-react-hooks haven't released
# stable ESLint 10-compatible versions yet
legacy-peer-deps=true
